import { NextPage } from "next";
import React from "react";

const IndexPage: NextPage = () => {
  return (
    <h1 className="container mx-auto">
      Build your full text, faceted search site with google sheet as database
    </h1>
  );
};

export default IndexPage;
